# 點擊日誌設定（一次性）

讓行事曆網頁把「使用者點了什麼」匿名寫進你自己的 Google 試算表。整個設定只做一次，之後要開關只要改 `data/設定.csv`。

> 隱私：只記匿名資料 —— 一組隨機 session 碼（用來分辨幾台裝置）、點了哪個動作、裝置螢幕寬度、瀏覽器語言、來源頁。**不含姓名、帳號、IP、任何個資。**

---

## 步驟 1　建立 Google 試算表

1. 到 <https://sheets.google.com> 用你的 Google 帳號新建一個空白試算表。
2. 命名為「**國三行事曆 點擊日誌**」。
3. 第一列（A1 起）填表頭，共 8 欄：

   | A | B | C | D | E | F | G | H |
   |---|---|---|---|---|---|---|---|
   | 時間 | session | action | detail | view | screenW | lang | referrer |

## 步驟 2　貼上收集程式（Apps Script）

1. 在試算表上方選單：**擴充功能 → Apps Script**。
2. 把預設內容全部刪掉，貼上下面這段，存檔（💾）：

```javascript
// 收到網頁送來的點擊，append 一列到目前這份試算表。
function doPost(e) {
  try {
    var data = JSON.parse(e.postData.contents);

    // 簡單防灌：缺 action 就丟掉
    if (!data || !data.action) {
      return ContentService.createTextOutput("skip");
    }

    // 共用 token（選用）：若你在下面填了 TOKEN，網頁端也要帶相同的值才會記錄
    var TOKEN = ""; // 想加保護就改成自訂字串，並同步填到 設定.csv（見 README 步驟 4 附註）
    if (TOKEN && data.token !== TOKEN) {
      return ContentService.createTextOutput("bad-token");
    }

    var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
    var now = Utilities.formatDate(new Date(), "Asia/Taipei", "yyyy-MM-dd HH:mm:ss");

    sheet.appendRow([
      now,
      data.session || "",
      data.action || "",
      JSON.stringify(data.detail || {}),
      data.view || "",
      data.screenW || "",
      data.lang || "",
      data.referrer || ""
    ]);

    return ContentService.createTextOutput("ok");
  } catch (err) {
    return ContentService.createTextOutput("error");
  }
}
```

## 步驟 3　部署成網頁應用程式

1. 右上角 **部署 → 新增部署**。
2. 齒輪 ⚙️ 選類型 **網頁應用程式**。
3. 設定：
   - **執行身分**：我（你的帳號）
   - **誰可以存取**：**任何人**（網頁要能匿名送資料，所以必須是「任何人」）
4. 按 **部署**，第一次會要你授權（選你的帳號 → 進階 → 前往專案 → 允許）。
5. 複製產生的 **網頁應用程式網址**，長得像：
   `https://script.google.com/macros/s/AKfy……/exec`

## 步驟 4　把網址填進行事曆

1. 打開 `data/設定.csv`，找到這一列：

   ```
   點擊日誌端點,,留空＝不記錄點擊；...
   ```

2. 把網址貼到中間（第二欄 value）：

   ```
   點擊日誌端點,https://script.google.com/macros/s/AKfy……/exec,留空＝不記錄點擊；...
   ```

3. 存檔（記得用「CSV UTF-8」格式，見 `維護指南.md`）。
4. 執行 `node tools/build.mjs`，再雙擊 `發布.bat` / `發布.command` 發布。

完成後，學生家長每次在網頁上的點擊都會出現在你的試算表裡。

---

## 平常怎麼用

- **看日誌**：直接打開那份 Google 試算表，每一列就是一次點擊，依時間排序。
- **暫停記錄**：把 `設定.csv` 的 `點擊日誌端點` 值清空，重新 build 發布即可。
- **做統計**（選用）：在試算表開第二個分頁，用公式快速彙整：
  - 各動作次數：`=QUERY(工作表1!A:H, "select C, count(C) group by C order by count(C) desc label count(C) '次數'")`
  - 每天開啟頁面次數：`=QUERY(工作表1!A:H, "select C, count(C) where C='page-open' group by C")`（或對 A 欄日期做樞紐分析表）
  - 也可用「插入 → 樞紐分析表」拖 `action`、`時間` 自由統計。

## action 代表什麼

`action` 欄是網頁上的動作代號，常見的有：

| action | 意思 |
|---|---|
| `page-open` | 開啟頁面（detail 裡 view 是列表 list 還是月曆 month） |
| `set-view` | 切換列表／月曆（detail.view） |
| `open-event` | 點開某個行程（detail.eventId） |
| `open-round` | 點開某回高名模考（detail.round） |
| `month-pick` / `select-date` | 點某一天（detail.date） |
| `month-prev` / `month-next` | 月曆上/下一月 |
| `set-subject` | 科目快選（detail.subject） |
| `open-menu` / `open-notices` / `open-timetable` | 開選單／通知／課表視窗 |
| `copy-event` | 複製行程資訊 |
| `jump-today` | 按「今天」 |

## 安全性小提醒

端點網址會出現在公開的網頁原始碼裡（這對 Apps Script 是正常用法），理論上有人可以照著灌假資料。若真的發生：

- 在 Apps Script 的 `TOKEN` 填一個自訂字串擋掉亂送的請求（要擋得更徹底可再請我把網頁端一起帶上 token）。
- 或直接 **重新部署 → 管理部署 → 封存舊版本**，換一個新網址再填回 `設定.csv`。
