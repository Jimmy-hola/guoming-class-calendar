# 維護指南

這份給日常維護使用：先改 `data/` 裡的 CSV，改完重建預覽，確認沒問題再發布。

## 重建與發布

1. 修改需要的 CSV。
2. 執行 `node tools/build.mjs`，產生新的 `docs/events.json`。
3. 執行 `node tools/serve.mjs` 或雙擊 `預覽.bat` / `預覽.command`，開本機預覽確認。
4. 確認後雙擊 `發布.bat` / `發布.command` 發布到 GitHub Pages。

## 常見修改

### 改高名模考日期或範圍

改 `data/高名模考.csv` 對應那一列：

- `date`：本班實際考試日期。
- `subject`：科目。
- `round_label`：第幾回。
- `range`：範圍。
- `official_date`：官方模考日。
- `paper_provider`、`notes`：卷別與備註。

模考週一到週五不要排複習卷；build 時若撞到會警告。

### 停課、國定假日、備註

改 `data/特殊日.csv`：

- `date_start`、`date_end`：開始與結束日期。
- `type`：確認停課填 `停課`；只提醒、不停課填 `備註`。
- `title`：行事曆顯示標題。
- `notes`：補充說明。
- `pinned`：填 `是` 可讓這筆通知不受天數限制、固定顯示（見「釘選」），平常留空。

目前國定假日先以 `備註` 暫定。若確認停課，把該列 `type` 改成 `停課`；若照常上課，刪除該列。

### 改網頁或紙本公布範圍

改 `data/設定.csv`：

- `公布截止日`：網頁只公布到這一天；之後資料不會輸出到 `docs/events.json`。
- `紙本公布截止日`：`docs/print.html` 只印到這個月份；留空時同 `公布截止日`。

確認後重新執行 `node tools/build.mjs`。

### 改「重要通知」顯示幾天內的內容

首頁的「重要通知」和點「查看全部」的通知面板，各自只列出「從今天起算一段天數內」的通知。天數在 `data/設定.csv` 調整：

- `首頁通知天數`：首頁那塊只列未來幾天內（含今天），預設 14（兩週）。
- `通知面板天數`：「查看全部」面板列未來幾天內，預設 30（約一個月）。

留空就用預設值。調完重新 `node tools/build.mjs`。
（注意：超過天數的通知不是被刪掉，只是「還沒到、暫時不列」；時間接近時自然會出現。例如暑假、8 月模考要等接近才會進通知。想讓某筆提早常駐，用下面的「釘選」。）

### 釘選某筆通知讓它一直顯示（pinned）

如果某筆通知很重要、想讓它不管多遠都固定出現在通知裡（不受上面的天數限制），在該列的 `pinned` 欄填 `是`（或 `1`、`Y`）。留空就是不釘選。

- 可用的檔：`data/重要日程.csv`、`data/特殊日.csv`、`data/高名模考.csv`，最後一欄都是 `pinned`。
- 釘選只會「突破天數上限」，仍然只顯示今天以後的；過去的通知不會被翻出來。
- 不想釘了就把 `是` 清空。

改完一樣 `node tools/build.mjs`。

### 新增各校段考、畢旅、重要日程

改 `data/重要日程.csv`，欄位是：

- `date_start`
- `date_end`
- `title`
- `短標籤`：月曆小格顯示的短文字，例如 `考前特訓`、`數學課`、`新課表`。留空時會自動從 `title` 取前幾個字。
- `notes`
- `pinned`：填 `是` 可讓這筆固定顯示在通知裡（見「釘選」），平常留空。

日期可填同一天，也可以填跨日區間。

### 改複習卷進度

改 `data/複習卷進度.csv`：

- `date`
- `start_time`
- `end_time`
- `subject`
- `round_no`
- `notes`

範圍會自動從 `data/複習卷清單.csv` 帶入；不要在進度檔手動改範圍。

### 改每週課表

改 `data/課表.csv`：

- `weekday`
- `start_time`
- `end_time`
- `subject`
- `activity`
- `notes`

每週課表顯示在網頁的「每週課表」視窗，不會每天重複出現在行事曆主列表。

## 用 Excel 編輯 CSV 的兩個地雷

1. **編碼**：`data/*.csv` 都要保持 UTF-8 含 BOM。另存請選「CSV UTF-8 (逗號分隔)」，不要選一般 CSV，避免中文變亂碼。
2. **日期格式**：日期欄一律維持 `YYYY-MM-DD`（破折號，例如 `2026-08-17`）。Excel 開檔／存檔時很容易把它自動轉成 `2026/8/17`（斜線）；斜線 build 會自動轉回，但若被改成怪日期就會報錯。`設定.csv` 的日期尤其要注意。

> 若整個檔只有 Excel 亂改、沒有你要的修改，可用 `git checkout -- data/<檔名>.csv` 還原。

## build 的自動檢查（少踩上面的雷）

`node tools/build.mjs` 開頭會先自我檢查，問題分兩種處理：

- **自動修好、繼續跑**：CSV 缺了 BOM 會自動補回（畫面顯示 `🩹 已自動補回 UTF-8 BOM：…`）。所以 Excel 存檔弄丟 BOM 多半不用你手動處理，重新 build 就好。
- **紅字報錯、停止不產生**（exit 1，`docs/events.json` 不會被動到，修好再 build）：
  - 檔案被另存成 Big5 等錯誤編碼（內容已是亂碼）→ 請用「CSV UTF-8」重新存檔。
  - 日期不是有效的 `YYYY-MM-DD`（例如打錯成不存在的日期）。
  - 時間不是 `HH:MM`。
  - 科目名稱不對：`複習卷進度.csv` 要與 `複習卷清單.csv` 一致（公民＝「公民與社會」）；`高名模考.csv`、`暑訓進度.csv` 要用大科 國文／英文／數學／自然／社會。

  錯誤訊息會指出是哪個檔、第幾行、哪個欄位，照著改即可。
